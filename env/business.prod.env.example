# Business (Next.js) PRODUCTION runtime env.
# Copy to /opt/onfood-prod/env/business.prod.env.
# NEXT_PUBLIC_* values are baked into the image by CI.

NODE_ENV=production
BACKEND_URL=http://business-api:8002
YANDEX_GEOCODER_API_KEY=
