# Shared proxy headers for all onfood-dev upstream blocks.
# Installed to /etc/nginx/snippets/onfood-proxy.conf on the server.

proxy_http_version 1.1;
proxy_set_header Host $host;
proxy_set_header X-Real-IP $remote_addr;
proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $scheme;
proxy_set_header X-Forwarded-Host $host;

proxy_set_header Upgrade $http_upgrade;
proxy_set_header Connection "upgrade";

# proxy_read_timeout is set per-vhost (frontends use 300s; API uses the nginx
# default). Kept out of the snippet to avoid duplicate-directive errors.
proxy_send_timeout 60s;
proxy_connect_timeout 5s;
